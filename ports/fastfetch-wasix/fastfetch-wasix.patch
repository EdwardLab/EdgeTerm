diff --git a/CMakeLists.txt b/CMakeLists.txt
index ac2315e..c3bc508 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -45,6 +45,8 @@ elseif("${CMAKE_SYSTEM_NAME}" STREQUAL "Haiku")
     set(Haiku TRUE CACHE BOOL "..." FORCE)
 elseif("${CMAKE_SYSTEM_NAME}" STREQUAL "GNU")
     set(GNU TRUE CACHE BOOL "..." FORCE)
+elseif("${CMAKE_SYSTEM_NAME}" STREQUAL "WASI")
+    set(GNU TRUE CACHE BOOL "..." FORCE)
 elseif(NOT APPLE AND NOT WIN32)
     message(FATAL_ERROR "Unsupported platform: ${CMAKE_SYSTEM_NAME}")
 endif()
@@ -1311,21 +1313,14 @@ elseif(GNU)
         src/detection/bluetooth/bluetooth_linux.c
         src/detection/bluetoothradio/bluetoothradio_linux.c
         src/detection/codec/codec_linux.c
-        src/detection/disk/disk_linux.c
+        src/detection/disk/disk_nosupport.c
         src/detection/dns/dns_linux.c
         src/detection/physicaldisk/physicaldisk_nosupport.c
         src/detection/physicalmemory/physicalmemory_nosupport.c
         src/detection/diskio/diskio_nosupport.c
-        src/detection/displayserver/linux/displayserver_linux.c
-        src/detection/displayserver/linux/wayland/wayland.c
-        src/detection/displayserver/linux/common.c
-        src/detection/displayserver/linux/drm.c
-        src/detection/displayserver/linux/wmde.c
-        src/detection/displayserver/linux/xcb.c
-        src/detection/displayserver/linux/xlib.c
+        src/detection/displayserver/displayserver_wasix.c
         src/detection/font/font_linux.c
-        src/detection/gpu/gpu_gnu.c
-        src/detection/gpu/gpu_pci.c
+        src/detection/gpu/gpu_nosupport.c
         src/detection/gtk_qt/gtk.c
         src/detection/host/host_nosupport.c
         src/detection/icons/icons_linux.c
@@ -1333,9 +1328,9 @@ elseif(GNU)
         src/detection/keyboard/keyboard_nosupport.c
         src/detection/libc/libc_linux.c
         src/detection/lm/lm_linux.c
-        src/detection/loadavg/loadavg_linux.c
+        src/detection/loadavg/loadavg_nosupport.c
         src/detection/locale/locale_linux.c
-        src/detection/localip/localip_linux.c
+        src/detection/localip/localip_wasix.c
         src/detection/gamepad/gamepad_nosupport.c
         src/detection/media/media_linux.c
         src/detection/memory/memory_linux.c
@@ -1356,7 +1351,7 @@ elseif(GNU)
         src/detection/theme/theme_linux.c
         src/detection/tpm/tpm_nosupport.c
         src/detection/uptime/uptime_linux.c
-        src/detection/users/users_linux.c
+        src/detection/users/users_nosupport.c
         src/detection/wallpaper/wallpaper_linux.c
         src/detection/wifi/wifi_nosupport.c
         src/detection/wm/wm_linux.c
@@ -1599,7 +1594,7 @@ endif()
 if(NOT BINARY_LINK_TYPE STREQUAL "dlopen")
     message(STATUS "Enabling custom link type: ${BINARY_LINK_TYPE}")
     target_compile_definitions(libfastfetch PUBLIC FF_DISABLE_DLOPEN=1)
-    if(NOT WIN32)
+    if(NOT WIN32 AND NOT "${CMAKE_SYSTEM_NAME}" STREQUAL "WASI")
         set(CMAKE_EXE_LINKER_FLAGS "${CMAKE_EXE_LINKER_FLAGS} -Wl,--copy-dt-needed-entries")
     endif()
 endif()
diff --git a/src/common/impl/FFPlatform_unix.c b/src/common/impl/FFPlatform_unix.c
index 3b9c87d..37ead49 100644
--- a/src/common/impl/FFPlatform_unix.c
+++ b/src/common/impl/FFPlatform_unix.c
@@ -142,6 +142,8 @@ static void getExePath(FFPlatform* platform) {
             break;
         }
     }
+#elif defined(__wasi__)
+    size_t exePathLen = 0;
 #endif
     if (exePathLen > 0) {
         ffStrbufEnsureFree(&platform->exePath, PATH_MAX);
diff --git a/src/detection/displayserver/displayserver_wasix.c b/src/detection/displayserver/displayserver_wasix.c
new file mode 100644
index 0000000..a9516d4
--- /dev/null
+++ b/src/detection/displayserver/displayserver_wasix.c
@@ -0,0 +1,6 @@
+#include "displayserver.h"
+
+void ffConnectDisplayServerImpl(FFDisplayServerResult* ds)
+{
+    (void) ds;
+}
diff --git a/src/detection/localip/localip_wasix.c b/src/detection/localip/localip_wasix.c
new file mode 100644
index 0000000..0624f66
--- /dev/null
+++ b/src/detection/localip/localip_wasix.c
@@ -0,0 +1,8 @@
+#include "localip.h"
+
+const char* ffDetectLocalIps(const FFLocalIpOptions* options, FFlist* results)
+{
+    (void) options;
+    (void) results;
+    return "Local address detection is not available in this runtime";
+}
diff --git a/src/detection/packages/packages.h b/src/detection/packages/packages.h
index 2119349..954b61a 100644
--- a/src/detection/packages/packages.h
+++ b/src/detection/packages/packages.h
@@ -66,7 +66,7 @@ const char* ffDetectPackages(FFPackagesResult* result, FFPackagesOptions* option
 bool ffPackagesReadCache(FFstrbuf* cacheDir, FFstrbuf* cacheContent, const char* filePath, const char* packageId, uint32_t* result);
 bool ffPackagesWriteCache(FFstrbuf* cacheDir, FFstrbuf* cacheContent, uint32_t num_elements);
 
-#if defined(__linux__) || defined(__APPLE__) || defined(__GNU__)
+#if defined(__linux__) || defined(__APPLE__) || defined(__GNU__) || defined(__wasi__)
 uint32_t ffPackagesGetNix(FFstrbuf* baseDir, const char* dirname);
 #endif
 #ifndef _WIN32
diff --git a/src/detection/uptime/uptime_linux.c b/src/detection/uptime/uptime_linux.c
index 75c7768..ab3cb9b 100644
--- a/src/detection/uptime/uptime_linux.c
+++ b/src/detection/uptime/uptime_linux.c
@@ -25,8 +25,13 @@ const char* ffDetectUptime(FFUptimeResult* result) {
 #endif
 #ifndef __GNU__
     struct timespec uptime;
-    if (clock_gettime(CLOCK_BOOTTIME, &uptime) != 0) {
-        return "clock_gettime(CLOCK_BOOTTIME) failed";
+    #ifdef __wasi__
+    const clockid_t uptimeClock = CLOCK_MONOTONIC;
+    #else
+    const clockid_t uptimeClock = CLOCK_BOOTTIME;
+    #endif
+    if (clock_gettime(uptimeClock, &uptime) != 0) {
+        return "clock_gettime failed";
     }
 
     result->uptime = (uint64_t) uptime.tv_sec * 1000 + (uint64_t) uptime.tv_nsec / 1000000;
